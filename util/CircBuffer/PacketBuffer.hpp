//TODO implement a sub class of circbuffer
//supplys constructor with our constants
//if we add something that isn't as big as our PACKET_DATA const
//then we set the buffer to dump mode, where it will always say the next buffer
//is ready
